import {BACKEND_PORT} from "./constants";

export const loginOperationPath = `http://localhost:${BACKEND_PORT}/users/login`;
export const projectOperationPath = `http://localhost:${BACKEND_PORT}/projectOperation/projectList`;